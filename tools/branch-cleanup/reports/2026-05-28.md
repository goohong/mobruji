# branch cleanup sweep — 2026-05-28

- 실행 시각: `20260528T013155Z`
- 모드: APPLY=0 (0=dry-run, 1=apply)
- 기준: merged≥30d / closed-nomerge≥30d / stale≥60d / active-protect=7d

## 요약

| 카테고리 | 후보 수 |
|---|---:|
| merged-30d+            | 0 |
| closed-nomerge-30d+  | 0 |
| stale-60d+              | 0 |
| agent-worktree            | 0 |
| **TOTAL branches**        | **0** |

## branch / worktree 후보

_후보 없음._

## 권고 행동

1. branch 5 카테고리 머지 30일+ + closed-nomerge 30일+ → `--apply --yes` 적용 가능.
2. stale 60일+ branch — 운영자 sample 검토 후 `--apply`.
3. closed issue → `--issues --apply` 로 `stale` 라벨 부착 (revert 가능).

## 안티패턴

- `git branch -D` / `git push --force-with-lease` 직접 호출 금지 — 본 스크립트 통해.
- `.claude/worktrees/agent-*` 도 `rm -rf` 직접 X — `git worktree remove` 사용.
