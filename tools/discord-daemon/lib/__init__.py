"""discord-daemon lib package — shared modules (Python + shell hook).

Python modules:
- forum_template_validator: PR F (forum-starter-template-guard) — validate
  forum thread starter body against template marker SoT.
"""
