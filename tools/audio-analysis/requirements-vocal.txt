# 옵션 — Spleeter vocal stem 분리 의존성 (analyze.py --vocal-separation).
#
# 본 의존성은 메인 requirements.txt 와 분리한다:
#   - spec docs/features/song-self-analysis-pipeline.md §10-2: default = vocal-skip.
#     Spleeter 도입 여부는 ADR-0015 트리거 충족 시 결정. CI 빌드 시간 회귀 방지를
#     위해 메인 경량 경로(yt-dlp+librosa)와 격리한다.
#   - TensorFlow 의존이 무거워 별도 venv/컨테이너에서 설치하는 것을 권장한다.
#   - pretrained model 캐시는 AUDIO_ANALYSIS_MODEL_DIR(예: NCP /data 영속 볼륨)로
#     외부화한다 — 매 실행 재다운로드 방지.
#
# 설치:
#   pip install -r requirements.txt -r requirements-vocal.txt
spleeter==2.4.0
